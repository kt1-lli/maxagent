[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: 标记慢测试（如 release pipeline 端到端，约 60s/ABI）。
        默认运行；CI 上想加速可用 -m 'not slow' 跳过。
