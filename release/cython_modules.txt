# Cython 编译白名单（Tier-1 + Tier-2，共 8 个文件）
#
# 格式：每行一个相对于 maxagent/ 包根的源文件路径
# 注释以 # 开头，空行忽略
#
# 修改本清单只影响 build.py 选择哪些文件走 Cython 路径，
# 不需要修改 build.py 主体。需要扩缩范围只改这一份清单。
#
# Tier-1（最高保护价值，~1700 行）：含系统 prompt / LLM key 处理 / 对话编排
agent/conversation.py
agent/coding_rules.py
agent/worker.py
llm_client.py
sessions.py

# Tier-2（中等保护价值，~1300 行）：调度安全 / 配置加密 / 工具注册
tools/dispatcher.py
config.py
# tools/registry.py  # 已从白名单移除，详见下方"已知问题"

# 注意事项：
# - agent/worker.py 含 14 处 PySide Signal/Slot，build.py 会自动启用
#   `binding=True` 以保证 Qt MetaObject 反射正常。
# - 列表中的文件不能是 __init__.py（包入口必须保留 .py 明文）。
#
# 已知问题（2026-05-25）：
# - tools/registry.py **不能** 走 Cython。原因：该文件提供 @tool 装饰器，
#   会在每个工具模块（scene_query / geometry / ...）import 阶段被多次
#   调用，内部再用 inspect.signature(func) 反射用户函数。
#   即便启用了 binding=True + embedsignature=True，Cython 编译后的
#   <cyfunction tool> 在 3ds Max 嵌入 Python (3.11.x) 环境里反射纯
#   .pyc 函数时仍会触发 C 层崩溃（Max 桥接报 "Unknown exception"，
#   Python try/except 无法拦截）。
#   该问题在 Linux 沙箱 + 系统 CPython 上不复现，仅在 Max 嵌入解释器
#   触发，疑似 cyfunction frame 与 CPython frame 跨边界访问的环境
#   敏感问题。registry.py 是纯框架代码（装饰器 + JSON Schema 推导），
#   不含敏感逻辑或密钥，明文 .pyc 兜底无安全损失。
